require 'spec_helper'

describe Moltrio::Config do
  it 'has a version number' do
    expect(Moltrio::Config::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
